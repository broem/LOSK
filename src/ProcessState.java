// a novel class but ultimately worthless
public class ProcessState {
    public enum state{ // will be used as flags for our newQueue in scheduler class
        NEW, READY, RUN, WAIT, EXIT
    }



}
